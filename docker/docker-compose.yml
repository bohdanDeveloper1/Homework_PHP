version: "3"

services:
  http:
    image: nginx
    ports:
      - 80:80
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:cached
      - ./../src:/var/www/localhost
    links:
      - php

  php:
    build: php
    volumes:
      - ./../src:/var/www/localhost
